diff --git a/packages/expo-swift-term/ios/AccessibilityService.swift b/packages/expo-swift-term/ios/AccessibilityService.swift
new file mode 100644
index 0000000..3963944
--- /dev/null
+++ b/packages/expo-swift-term/ios/AccessibilityService.swift
@@ -0,0 +1,5 @@
+import Foundation
+
+final class AccessibilityService {
+  func invalidate() {}
+}
diff --git a/packages/expo-swift-term/ios/ExpoSwiftTerm.podspec b/packages/expo-swift-term/ios/ExpoSwiftTerm.podspec
new file mode 100644
index 0000000..443edfa
--- /dev/null
+++ b/packages/expo-swift-term/ios/ExpoSwiftTerm.podspec
@@ -0,0 +1,25 @@
+Pod::Spec.new do |s|
+  s.name           = 'ExpoSwiftTerm'
+  s.version        = '0.1.0'
+  s.summary        = 'Native SwiftTerm terminal view for Expo'
+  s.homepage       = 'https://github.com/sethwebster/expo-swift-term'
+  s.license        = 'MIT'
+  s.author         = 'Seth Webster'
+  s.source         = { git: '' }
+  s.platform       = :ios, '15.1'
+  s.swift_version  = '5.9'
+  s.source_files   = [
+    '**/*.swift',
+    'vendor/SwiftTerm/*.swift',
+    'vendor/SwiftTerm/Apple/*.swift',
+    'vendor/SwiftTerm/Apple/Metal/*.swift',
+    'vendor/SwiftTerm/iOS/*.swift',
+  ]
+  s.exclude_files  = [
+    'vendor/SwiftTerm/Mac/**/*',
+    'vendor/SwiftTerm/Documentation.docc/**/*',
+  ]
+  s.resources      = ['vendor/SwiftTerm/Apple/Metal/Shaders.metal']
+
+  s.dependency 'ExpoModulesCore'
+end
diff --git a/packages/expo-swift-term/ios/ExpoSwiftTermView.swift b/packages/expo-swift-term/ios/ExpoSwiftTermView.swift
new file mode 100644
index 0000000..c997a66
--- /dev/null
+++ b/packages/expo-swift-term/ios/ExpoSwiftTermView.swift
@@ -0,0 +1,144 @@
+import ExpoModulesCore
+
+class ExpoSwiftTermView: ExpoView, TerminalViewDelegate {
+  private var terminalView: TerminalView!
+  private var currentFontSize: CGFloat = 14
+  private var currentFontFamily: String = "Menlo"
+
+  let onData = EventDispatcher()
+  let onResize = EventDispatcher()
+  let onTitleChange = EventDispatcher()
+  let onBell = EventDispatcher()
+
+  required init(appContext: AppContext? = nil) {
+    super.init(appContext: appContext)
+    setupTerminal()
+  }
+
+  private func setupTerminal() {
+    let font = UIFont(name: currentFontFamily, size: currentFontSize)
+      ?? UIFont.monospacedSystemFont(ofSize: currentFontSize, weight: .regular)
+
+    terminalView = TerminalView(frame: bounds, font: font)
+    terminalView.terminalDelegate = self
+    terminalView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
+    terminalView.becomeFirstResponder()
+    addSubview(terminalView)
+  }
+
+  override func layoutSubviews() {
+    super.layoutSubviews()
+    terminalView.frame = bounds
+  }
+
+  // MARK: - Props
+
+  func updateFontSize(_ size: CGFloat) {
+    currentFontSize = size
+    applyFont()
+  }
+
+  func updateFontFamily(_ family: String) {
+    currentFontFamily = family
+    applyFont()
+  }
+
+  private func applyFont() {
+    let font = UIFont(name: currentFontFamily, size: currentFontSize)
+      ?? UIFont.monospacedSystemFont(ofSize: currentFontSize, weight: .regular)
+    terminalView.font = font
+  }
+
+  func updateForegroundColor(_ hex: String) {
+    if let color = UIColor(hex: hex) {
+      terminalView.nativeForegroundColor = color
+    }
+  }
+
+  func updateBackgroundColor(_ hex: String) {
+    if let color = UIColor(hex: hex) {
+      terminalView.nativeBackgroundColor = color
+    }
+  }
+
+  // MARK: - Commands
+
+  func write(base64 data: String) {
+    guard let bytes = Data(base64Encoded: data) else { return }
+    terminalView.feed(byteArray: ArraySlice(bytes))
+  }
+
+  func writeText(_ text: String) {
+    terminalView.feed(text: text)
+  }
+
+  func resizeTerminal(cols: Int, rows: Int) {
+    terminalView.resize(cols: cols, rows: rows)
+  }
+
+  func getTerminalSize() -> [String: Int] {
+    let terminal = terminalView.getTerminal()
+    return ["cols": terminal.cols, "rows": terminal.rows]
+  }
+
+  // MARK: - TerminalViewDelegate
+
+  func send(source: TerminalView, data: ArraySlice<UInt8>) {
+    let base64 = Data(data).base64EncodedString()
+    onData(["data": base64])
+  }
+
+  func sizeChanged(source: TerminalView, newCols: Int, newRows: Int) {
+    onResize(["cols": newCols, "rows": newRows])
+  }
+
+  func setTerminalTitle(source: TerminalView, title: String) {
+    onTitleChange(["title": title])
+  }
+
+  func scrolled(source: TerminalView, position: Double) {
+    // No-op for now
+  }
+
+  func hostCurrentDirectoryUpdate(source: TerminalView, directory: String?) {
+    // No-op for now
+  }
+
+  func requestOpenLink(source: TerminalView, link: String, params: [String: String]) {
+    if let url = URL(string: link) {
+      DispatchQueue.main.async {
+        UIApplication.shared.open(url)
+      }
+    }
+  }
+
+  func bell(source: TerminalView) {
+    onBell()
+  }
+
+  func clipboardCopy(source: TerminalView, content: Data) {
+    if let text = String(data: content, encoding: .utf8) {
+      UIPasteboard.general.string = text
+    }
+  }
+
+  func rangeChanged(source: TerminalView, startY: Int, endY: Int) {
+    // No-op — could be used for partial render optimization
+  }
+}
+
+// MARK: - UIColor hex helper
+
+private extension UIColor {
+  convenience init?(hex: String) {
+    var h = hex.trimmingCharacters(in: .whitespacesAndNewlines)
+    if h.hasPrefix("#") { h.removeFirst() }
+    guard h.count == 6, let rgb = UInt64(h, radix: 16) else { return nil }
+    self.init(
+      red: CGFloat((rgb >> 16) & 0xFF) / 255,
+      green: CGFloat((rgb >> 8) & 0xFF) / 255,
+      blue: CGFloat(rgb & 0xFF) / 255,
+      alpha: 1
+    )
+  }
+}
