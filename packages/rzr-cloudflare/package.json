{
  "name": "@sethwebster/rzr-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler dev",
    "migrate": "npx wrangler d1 migrations apply rzr-auth --remote",
    "deploy": "npm run migrate && npx wrangler deploy",
    "test": "node --test"
  }
}
